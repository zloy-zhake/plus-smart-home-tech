spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shopping_store
    username: shopping_store
    password: shopping_store
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
