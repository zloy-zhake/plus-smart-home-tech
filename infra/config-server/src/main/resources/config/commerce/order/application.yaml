spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: order
    password: order
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
